name: build-push-containers

on:
  release:
    types: [published]
  push:
    branches:
      - 'main'
  workflow_dispatch:
    inputs:
      image_tag:
        description: >
          Tag to publish the manually-built behavior-dev/behavior-gha images under (e.g. the branch
          name).
        required: true
        type: string

jobs:
  docker:
    runs-on: docker-builder  # This is a larger runner with more disk space.
    steps:
      -
        name: Check disk space
        run: df . -h
      -
        name: Free disk space
        run: |
          sudo docker rmi $(docker image ls -aq) >/dev/null 2>&1 || true
          sudo rm -rf \
            /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup \
            /usr/lib/jvm /opt/hostedtoolcache/CodeQL || true
          echo "some directories deleted"
          sudo apt-get update >/dev/null 2>&1
          sudo apt install aptitude -y >/dev/null 2>&1
          sudo aptitude purge aria2 ansible azure-cli shellcheck rpm xorriso zsync \
            esl-erlang firefox gfortran-8 gfortran-9 google-chrome-stable \
            google-cloud-sdk imagemagick \
            libmagickcore-dev libmagickwand-dev libmagic-dev ant ant-optional kubectl \
            mercurial apt-transport-https mono-complete libmysqlclient \
            unixodbc-dev yarn chrpath libssl-dev libxft-dev \
            libfreetype6 libfreetype6-dev libfontconfig1 libfontconfig1-dev \
            snmp pollinate libpq-dev postgresql-client powershell ruby-full \
            sphinxsearch subversion mongodb-org azure-cli microsoft-edge-stable \
            -y -f >/dev/null 2>&1 || true
          sudo aptitude purge google-cloud-sdk -f -y >/dev/null 2>&1 || true
          sudo aptitude purge microsoft-edge-stable -f -y >/dev/null 2>&1 || true
          sudo apt purge microsoft-edge-stable -f -y >/dev/null 2>&1 || true
          sudo aptitude purge '~n ^mysql' -f -y >/dev/null 2>&1 || true
          sudo aptitude purge '~n ^php' -f -y >/dev/null 2>&1 || true
          sudo aptitude purge '~n ^dotnet' -f -y >/dev/null 2>&1 || true
          sudo apt-get autoremove -y >/dev/null 2>&1
          sudo apt-get autoclean -y >/dev/null 2>&1
          echo "some packages purged"
      -
        name: Check disk space
        run: |
          df . -h
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to NVCR
        uses: docker/login-action@v3
        with:
          registry: nvcr.io
          username: ${{ secrets.NVCR_USERNAME }}
          password: ${{ secrets.NVCR_PASSWORD }}
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
      -
        name: Metadata for prod Image
        id: meta-prod
        if: github.event_name != 'workflow_dispatch'
        uses: docker/metadata-action@v5
        with:
          images: |
            stanfordvl/behavior
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
      -
        name: Metadata for dev Image
        id: meta-dev
        uses: docker/metadata-action@v5
        with:
          images: |
            stanfordvl/behavior-dev
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=raw,value=${{ github.event.inputs.image_tag }},enable=${{ github.event_name == 'workflow_dispatch' }}
      -
        name: Metadata for actions Image
        id: meta-actions
        uses: docker/metadata-action@v5
        with:
          images: |
            stanfordvl/behavior-gha
          tags: |
            type=raw,value=latest,enable=${{ github.event_name != 'workflow_dispatch' }}
            type=raw,value=${{ github.event.inputs.image_tag }},enable=${{ github.event_name == 'workflow_dispatch' }}
      -
        name: Build and push prod image
        id: build-prod
        if: github.event_name != 'workflow_dispatch'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          provenance: false
          tags: ${{ steps.meta-prod.outputs.tags }}
          labels: ${{ steps.meta-prod.outputs.labels }}
          file: docker/Dockerfile
          cache-from: type=registry,ref=stanfordvl/behavior:build-cache
          cache-to: type=registry,ref=stanfordvl/behavior:build-cache,mode=max

      -
        name: Build and push dev image
        id: build-dev
        uses: docker/build-push-action@v5
        with:
          context: .
          build-args: "DEV_MODE=1"
          push: true
          provenance: false
          tags: ${{ steps.meta-dev.outputs.tags }}
          labels: ${{ steps.meta-dev.outputs.labels }}
          file: docker/Dockerfile
          # Manual workflow_dispatch builds read from the shared cache for speed but never write to
          # it, so a broken branch build can never poison the cache used by the real main/release
          # builds.
          cache-from: type=registry,ref=stanfordvl/behavior:build-cache
          cache-to: ${{ github.event_name != 'workflow_dispatch' && 'type=registry,ref=stanfordvl/behavior:build-cache,mode=max' || '' }}

      - name: Update actions image Dockerfile with dev image tag
        run: |
          sed -i "s/behavior-dev:latest/behavior-dev@${{ steps.build-dev.outputs.digest }}/g" docker/gh-actions/Dockerfile && cat docker/gh-actions/Dockerfile
      -
        name: Build and push actions image
        id: build-actions
        uses: docker/build-push-action@v5
        with:
          context: docker/gh-actions
          push: true
          tags: ${{ steps.meta-actions.outputs.tags }}
          labels: ${{ steps.meta-actions.outputs.labels }}
          file: docker/gh-actions/Dockerfile
          cache-from: type=registry,ref=stanfordvl/behavior:build-cache
          cache-to: ${{ github.event_name != 'workflow_dispatch' && 'type=registry,ref=stanfordvl/behavior:build-cache,mode=max' || '' }}
